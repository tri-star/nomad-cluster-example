# Increase log verbosity
log_level = "DEBUG"

# Setup data dir
data_dir = "/var/lib/nomad/client"


client {
    enabled = true
    servers = [ "{{ self_ip }}" ]
{% if env == 'vagrant' %}
    network_interface = "eth1"
{% endif %}
}

ports {
    http = 5656
}
